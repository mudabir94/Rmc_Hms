Add new medicine Form Validation Checks. 
1. When user types the name of the medicine, the prog should check if its already present and show an alert telling the user that medicine is already in the DB. Thus Preventing the user from redundancy. 
2. Simply show the list of types to the user. He can only select from the given list. 
3. Adding details about the medicine is optional for now. 
4. On Pressing save, The medicine should be added to the database and a popup should be appeared telling the user that the medicine is added. 
5. The form Should be refreshed after the popup is closed. 
	-So that the user can add more medicine if he likes.. 
tahira.saudagar.

Question(uestion_text="asdas",pub_=timez0ne.nnow())

Question 
id, question_text, pub_date
1,ssssm,current